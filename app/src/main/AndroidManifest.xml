<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hidday.directfarming" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity
            android:name=".MyAuctions"
            android:label="@string/title_activity_my_auctions" >
        </activity>
        <activity
            android:name=".ActiveEvents"
            android:label="@string/title_activity_active_auctions" >
        </activity>
        <activity
            android:name=".MarketEventDetails"
            android:label="@string/title_activity_market_event_details"
            android:parentActivityName=".ActiveEvents" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActiveEvents" />
        </activity>
        <activity
            android:name=".PlaceBid"
            android:label="@string/title_activity_place_bid" >
        </activity>
    </application>

</manifest>
